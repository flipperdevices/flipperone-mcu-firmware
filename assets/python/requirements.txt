# Packages for the assets build (image_convert.py, nanopb generator).
# Installed into .venv by run_venv.sh/.cmd on first run; the venv.stamp step
# in assets/CMakeLists.txt re-installs when this file changes.
pypng
lz4
pillow
protobuf
